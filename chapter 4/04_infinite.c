#include <stdio.h>

int main(){
    int i = 0;// i=iteration
    while(2<10){// condition will always be true condition will not stop
        printf("the value of i is %d\n", i);
        i++;
    }
    return 0;
}
