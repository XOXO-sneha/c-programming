#include <stdio.h>

int main(){
    int i =0;// i=iteration
    while(i<10){// condition will be checked until i reaches to 10 
        printf("the value of i is %d\n", i);
        i++;
    }

    return 0;
}
