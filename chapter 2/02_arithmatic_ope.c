#include <stdio.h>

int main(){
    int a= 3;
    int b= 4;
    int c = a+b;
    printf("the value of a is %d and value of b is %d and the sum c is %d",a,b,c);
    //modulus operator is used to get the reminder
    printf("the reminder when a is divided by b is:%d\n",a%b);

    // this doesnot work for expo in c
    // int d = a^b;
    return 0;
}
